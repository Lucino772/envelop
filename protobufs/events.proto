syntax = "proto3";

option go_package = "github.com/Lucino772/envelop/pkg/protobufs";

message Event {
    message ProcessLog {
        string log = 1;
    }

    oneof event {
        ProcessLog process_log_event = 1;
    }
}
